# Google Apps Script — clasp (never commit OAuth token store from home dir;
# this ignores a copy if it is ever placed in the project root)
.clasprc.json

# Optional: omit script ID from the repo (remove this line if you want .clasp.json tracked)
.clasp.json

# Local env / secrets (if you add Node/other tooling later)
.env
.env.*
!.env.example

# OS / editors
.DS_Store
*.swp
.idea/
.vscode/

# Cursor local workspace
.cursor/
